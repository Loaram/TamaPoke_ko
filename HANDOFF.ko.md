# TamaPoke 한국어판 — 다른 PC에서 이어가기

기록일: 2026-09-04. 다음 작업은 **실기 테스트 결과를 반영하는 것**입니다.

## 현재 상태

| 항목 | 확정된 내용 |
|---|---|
| 작업 저장소 | https://github.com/Loaram/TamaPoke_ko |
| 작업 기준 | DylanPDao/TamaPoke `6cd04669572fb2980d8520a74ed7f5a4c86dbadb`, 원본 v3.11 |
| 범위 | 전국도감 1025종·도감 9개 지방, 체육관 7개 지방. |
| 한국어 펌웨어 | **ko.1.1.0** |
| 현재 코드·펌웨어 | `ko.1.1.0` / `main` |
| 기본 브랜치 | `main` (한국어 작업 브랜치 `feat/korean`도 보존) |
| 공개 설치 페이지 | https://loaram.github.io/TamaPoke_ko/ |
| 실기 확인 | **아직 실시하지 않음. 사용자가 테스트 후 결과를 제공할 예정** |

ko.1.1.0은 가라르·팔데아 포켓몬을 더해 전국도감 1025종을 지원합니다. 성장 속도는 20분당 1레벨, 수면 중 활력 회복은 분당 15이며 조기 돌봄 종료의 다음 포켓몬 진화 지연은 제거했습니다. PMDCollab에 표시용 자료가 없는 48종과 그 계보를 합친 64종은 알 후보에서 제외합니다. 이전 버전에서 저장된 대기 알은 안전한 후보로 교체되고, 남아 있는 진화 지연 값은 0으로 정리됩니다. 이전 버전의 기록은 `docs/qa/`에 보존합니다.

각 정식 버전과 함께 비공개 PC 에뮬레이터 두 개를 만듭니다. `-dex`판은 1025종 일반 도감을 모두 열고, `-shiny`판은 일반·이로치 도감을 모두 엽니다. 예: `ko.1.1.0-dex`, `ko.1.1.0-shiny`. 각각 `tamapoke-dex.nvs`, `tamapoke-shiny.nvs`를 사용해 일반 에뮬레이터 저장과 분리합니다. 두 테스트판은 GitHub 릴리스와 설치 페이지에 올리지 않습니다.

## 완료한 작업

- 한국어 UI 173개, 포켓몬 1025종, 기술 테이블 136개 항목(빈 기술 포함), 18개 타입과 지방·트레이너·장소 이름.
- Galmuri11 부분 글꼴 695자, 비트맵 12,510바이트. UTF-8 디코딩·화면 정렬·긴 이름 폭 계산을 펌웨어와 에뮬레이터에서 공유.
- 기존 언어 인덱스 ES=0, EN=1, FR=2, DE=3, IT=4, PT=5를 유지하고 KO=6 추가. 새 세이브 기본값은 한국어, 기존 세이브의 언어 설정은 유지.
- 내부 포켓몬·기술 식별자, 저장 형식, 통신 형식 및 사용자 영문 별명 유지.
- Windows 한글 경로에서 에뮬레이터 스프라이트를 못 읽던 문제와 생성 도구의 문자 인코딩 문제 수정. Arduino 빌드는 영문 임시 경로에 스케치를 배치.
- 한국어 설치 페이지와 9개 TPAK 팩, 전송 응답·시간초과·중단 처리, Pages 자동 배포 구성.
- 원본 라이선스·크레딧 유지. Galmuri SIL OFL 라이선스 포함. 이름 데이터 출처는 `localization/README.md` 참고.

## 검증 결과와 한계

| 동일 도구로 로컬 빌드 | Dylan 원본 | 한국어판 |
|---|---:|---:|
| 프로그램 플래시 | 1,808,375 B | 1,849,923 B |
| 전역 RAM | 66,116 B | 66,884 B |

ko.1.1.0 한국어 앱 파일은 1,885,264바이트입니다. 스케치는 1,885,123바이트로 프로그램 영역 3,145,728바이트의 59%를 사용하며 전역 RAM은 66,940바이트입니다. 동적 메모리의 실기 최대 사용량은 측정하지 않았습니다.

- 이전 3.11-ko.1 [기본 브랜치 GitHub 전체 검사](https://github.com/Loaram/TamaPoke_ko/actions/runs/33709498914): 성공.
- 이전 3.11-ko.1 [GitHub Pages 배포](https://github.com/Loaram/TamaPoke_ko/actions/runs/33709498902): 성공.
- 한글 글꼴·문자열·이름·표시 폭, 세이브 복원, 이전 저장 형식 업그레이드, 통신 프로토콜 회귀 검사 통과.
- 독립 에뮬레이터 프로세스를 재시작해도 한국어 설정 유지. 실제 에뮬레이터 화면 12개 확인.
- 로컬 설치 페이지의 한국어 설치 버튼 활성화와 버전 표시 확인. 펌웨어 4개 파일의 SHA-256이 로컬 빌드와 일치. 9개 팩 구조와 크기 확인.
- 증거 파일: [docs/qa/ko-3.11.1](docs/qa/ko-3.11.1/README.md).

**실기 화면·터치·전원·SD·소리·무선 대전은 확인하지 않았습니다.** 에뮬레이터나 프로토콜 테스트 통과를 실기 성공으로 기록하지 마세요. 별명 입력 키보드와 ESP Web Tools의 팝업은 영문입니다.

## ko.1.0.1 변경 및 확인

- 돌봄 종료·진화·작별 확인창의 제목, 안내 두 줄, 버튼 간격 수정. 버튼 그림과 터치 영역은 동일한 상수를 공유합니다.
- 펌웨어·manifest·빌드 정보의 버전을 `ko.1.0.1`로 통일하고 설치 페이지의 버전 검사를 대응했습니다.
- ESP32-S3 펌웨어 빌드, 한국어 데이터·설치 파일 검사, 설치 페이지 전송 검사 통과.
- 실제 스케치의 버튼 영역·취소·방출 동작을 포함한 8개 실행 검사 통과.
- 호바귀 돌봄 종료, 진화, 작별 한국어 화면과 영어 확인창의 문구 간격 확인. [화면과 빌드 증거](docs/qa/ko.1.0.1/README.md).
- 원본 게임 데이터와 세이브 형식은 유지했습니다. 실기 테스트는 아직 미실시입니다.
- 한국어 화면을 넣은 14쪽 플레이 설명서(PDF·페이지별 이미지)는 설치 페이지 상단에서 다운로드할 수 있습니다. 배포 파일은 `web/guides/`에 있습니다. 설명서 편집 원본은 `ko.1.0.1` GitHub 릴리스의 `TamaPoke-ko.1.0.1-Play-Guide-Source.zip`입니다.

## ko.1.0.2 변경 및 확인

- 성장 속도를 실제 1시간당 1레벨에서 **45분당 1레벨**로 변경했습니다. 레벨 100은 3일 2시간 15분에 도달합니다.
- 수면 중 활력 회복을 **분당 +6에서 +10**으로 변경했습니다. 실시간 진행과 전원이 꺼져 있던 시간 계산에 같은 값을 적용했습니다.
- 조기 돌봄 종료의 진화 지연은 실시간 하루를 유지하도록 24레벨에서 32레벨로 자동 환산됩니다.
- 에뮬레이터 검사는 45분 경계, 레벨 상한, 실시간·오프라인 수면 회복 경로를 직접 확인합니다.
- 한국어 플레이 설명서와 설치 페이지의 버전·성장 안내를 ko.1.0.2 기준으로 갱신했습니다.
- ESP32-S3 펌웨어, Windows 에뮬레이터, 설치 파일은 로컬 및 GitHub 검사 결과를 기준으로 배포합니다. 실기 테스트는 아직 미실시입니다.
- 화면·밸런스·빌드 증거: [docs/qa/ko.1.0.2](docs/qa/ko.1.0.2/README.md).

## ko.1.0.3 변경 및 확인

- 성장 속도를 **40분당 1레벨**로 변경했습니다.
- 최종 진화 상태의 작별 제안 시점을 **3일에서 2일**로 줄였습니다. 2일째 레벨 73에서 작별이 열리고 레벨 100은 2일 18시간에 도달합니다.
- 수면 중 활력 회복은 ko.1.0.2와 동일하게 분당 +10입니다.
- 조기 돌봄 종료의 진화 지연은 실제 하루를 유지하도록 36레벨로 자동 환산됩니다.
- 에뮬레이터 검사는 40분 성장 경계, 2일 작별 경계, 레벨 상한, 실시간·오프라인 수면 회복을 직접 확인합니다.
- 화면·밸런스·빌드 증거: [docs/qa/ko.1.0.3](docs/qa/ko.1.0.3/README.md).

## ko.1.0.4 변경 및 확인

- 꼬시레(#767)의 원작 레벨 1 기술인 **벌레의저항**을 기술표와 한국어 이름표에 추가했습니다.
- ko.1.0.3에서 기술 없이 저장된 꼬시레도 업데이트 후 자동으로 기술을 복구합니다.
- 축약 기술표에 초반 기술이 없는 포켓몬은 기술 칸이 비지 않도록 임시 **발버둥**을 사용하며, 정상 레벨업 기술을 배우면 자동으로 교체됩니다.
- 809종 전부를 레벨 1과 레벨 100에서 생성하여 빈 기술 칸으로 전투가 막히지 않고 모든 기술 번호가 유효한지 검사합니다.
- 기본 GitHub README를 한국어로 바꾸고 원본 `socquique/TamaPoke` → 기반 포크 `DylanPDao/TamaPoke` → 한국어판 관계를 문서 맨 위에 명시했습니다.
- 에뮬레이터 화면과 검사 기록: [docs/qa/ko.1.0.4](docs/qa/ko.1.0.4/README.md).

## ko.1.0.5 변경 및 확인

- 첫 파트너 후보 3마리 아래에 **지방 선택** 버튼을 추가했습니다.
- 후보 확인 중에는 파트너가 확정되지 않으며, 다른 지방을 선택해 스타팅 포켓몬을 반복해서 비교할 수 있습니다.
- 이전에 선택한 지방이 있는 지역 선택 페이지로 돌아가므로 두 번째·세 번째 지역 페이지에서도 자연스럽게 이어집니다.
- 버튼은 실기 터치 최소 높이 44픽셀보다 큰 48픽셀이며 마지막 후보 영역과 겹치지 않습니다.
- 실제 선택 흐름, 한국어 화면, 466×466 에뮬레이터, 전체 회귀 검사와 ESP32-S3 펌웨어 빌드를 확인했습니다.
- 화면과 검사 기록: [docs/qa/ko.1.0.5](docs/qa/ko.1.0.5/README.md).

## ko.1.0.6 변경 및 확인

- 1~809번 범위에서 두 가지 이상의 결과가 있는 진화 12가족을 공통 분기 진화로 처리합니다.
- 냄새꼬, 슈륙챙이, 야돈, 이브이, 배루키, 개무소, 킬리아, 토중몬, 눈꼬마, 진주몽, 도롱충이, 코스모움이 대상입니다.
- 각 분기와 그 뒤의 진화 계보를 함께 확인해 도감에 없는 경로를 먼저 선택합니다. 모든 경로를 모은 뒤에는 가능한 결과 중 하나를 무작위로 선택합니다.
- 설치된 지방 팩에 그림이 없는 결과는 제외합니다. 가능한 분기가 없을 때에는 기존 기본 진화를 사용합니다.
- 코스모움이 솔가레오와 루나아라 양쪽으로 진화할 수 있고, 개무소는 최종 진화까지의 미등록 경로를 따라가는지 에뮬레이터 검사로 확인했습니다.
- 분기 진화 결과는 부화 후보에서 제외해 진화로만 수집하도록 정리했습니다. 포켓몬 번호, 세이브 및 통신 형식은 변경하지 않았습니다.
- 전체 회귀 검사, Windows 일반·도감 완성·전체 이로치 에뮬레이터 빌드와 ESP32-S3 펌웨어 빌드를 통과했습니다. 비공개 도감판 두 개는 배포하지 않습니다.
- 화면과 검사 기록: [docs/qa/ko.1.0.6](docs/qa/ko.1.0.6/README.md).

## ko.1.0.7 변경 및 확인

- 현재 배포 자료에 표시 이미지가 없는 19종을 알 후보에서 제외합니다.
- 해당 포켓몬으로 이어지는 앞 단계 9종도 함께 제외해, 키우다가 그림 없는 진화체가 되는 계보가 새 알에서 시작되지 않도록 했습니다.
- 이전 버전에서 이미 제외 대상이 대기 중인 알에 들어 있었다면 업데이트 후 안전한 후보로 교체하고 저장합니다.
- 이미 부화한 포켓몬과 도감 기록은 유지하며 포켓몬 번호, 세이브 및 통신 형식은 변경하지 않았습니다.
- 8개 지방과 모든 희귀도에서 9,600회 추첨하고 전체 진화 계보, 이전 저장의 대기 알 교체, 저장 재시작을 검사했습니다.
- 전체 회귀 검사, Windows 일반·도감 완성·전체 이로치 에뮬레이터 빌드와 ESP32-S3 펌웨어 빌드를 통과했습니다. 비공개 도감판 두 개는 배포하지 않습니다.
- 검사 기록: [docs/qa/ko.1.0.7](docs/qa/ko.1.0.7/README.md).

## ko.1.1.0a 변경 및 확인

- 가라르·팔데아 도감과 포켓몬 216종을 추가해 전국도감 1025종을 지원합니다. 가라르 범위에는 전국도감 연속성과 기존 포켓몬의 진화 연결을 위해 #899~#905 진화체도 포함합니다.
- 가라르·팔데아 스타팅 선택과 알 지역, 도감 지역을 추가했습니다. 체육관과 배지는 기존 관동~알로라 7지역 그대로입니다.
- 한국어 포켓몬 위키에서 공식 한국어 이름, 도감 번호, 타입, 종족값과 레벨업 기술을 확인해 저장한 `tools/gen89_data.json`을 생성 원본으로 사용합니다.
- PMDCollab의 일반 스프라이트 977종을 사용할 수 있습니다. 기본 그림이 없는 48종과 해당 진화 계보를 합친 64종은 알에서 나오지 않습니다.
- 가라르·팔데아 스프라이트의 종별 기여자와 라이선스는 [별도 출처표](docs/PMDCOLLAB_GALAR_PALDEA_CREDITS.md)에 기록했고 설치 페이지에서도 내려받을 수 있습니다.
- 네 자리 전국도감 번호의 스프라이트 파일을 읽도록 로더와 검사기를 확장했습니다.
- 일반·도감 완성·전체 이로치 Windows 에뮬레이터, 1025종 데이터·기술·진화·지역·저장 회귀 검사, ESP32-S3 펌웨어와 9개 팩 설치 페이지 검사를 통과했습니다.
- 비공개 `-dex`, `-shiny` 에뮬레이터는 로컬에만 만들며 릴리스와 Pages에는 포함하지 않습니다.
- 검사 기록: [docs/qa/ko.1.1.0a](docs/qa/ko.1.1.0a/README.md).

## ko.1.1.0 변경 및 확인

- 성장 속도를 20분당 1레벨로 변경했습니다. 레벨 100은 1일 9시간에 도달합니다.
- 수면 중 활력 회복을 실시간·오프라인 모두 분당 15로 높였습니다.
- 조기 돌봄 종료는 다음 포켓몬의 진화를 늦추지 않습니다. 이전 버전 저장에 남은 진화 지연 값도 불러올 때 제거합니다.
- 조기 돌봄 종료한 포켓몬이 파티에 남지 않고 다음 알에 작별 보너스를 주지 않는 기존 규칙은 유지합니다.
- Android Full 디버그 APK를 `ko.1.1.0-android.1`(versionCode 1102)로 만들었습니다. ARM64/x86_64와 9개 지역 팩을 포함하며 ZIP 무결성·페이지 정렬·v2/v3 서명·네이티브 진입점을 확인했습니다.
- 이전 `ko.1.1.0a-android.2` APK와 디버그 서명 키가 다릅니다. 설치된 이전 앱을 덮어쓰려면 원래 키스토어가 필요합니다. 앱을 삭제하면 Android 저장 데이터도 지워지므로 저장이 중요하면 삭제하지 마세요. 이번 키로 후속 APK를 계속 업데이트하려면 무시된 `build/android/debug.keystore`를 별도로 보관해야 합니다.
- 연결된 Android 기기가 USB 디버깅을 승인하지 않아 실제 기기 실행 확인은 아직 하지 않았습니다.
- 검사·빌드 기록: [docs/qa/ko.1.1.0](docs/qa/ko.1.1.0/README.md).

## 다른 PC에서 시작

새 PC의 Codex에서 이 GitHub 저장소를 열거나 클론하고 `AGENTS.md`, 이 문서, `README.ko.md`를 읽으면 됩니다. 이전 PC의 폴더 경로는 사용할 필요가 없습니다.

```sh
git clone https://github.com/Loaram/TamaPoke_ko.git
cd TamaPoke_ko
git switch main
git pull --ff-only
```

코드와 문서는 공개 저장소에서 읽을 수 있습니다. 수정사항을 올리려면 새 PC에서도 **Loaram 계정으로 GitHub 인증**이 필요합니다. 이전 PC의 로그인 정보는 저장소에 들어 있지 않습니다.

필요한 도구는 Git, Python 3.12 이상, Node.js, Arduino CLI입니다. Windows에서 실행한 Python 패키지는 Pillow 12.3.0, fonttools 4.64.0이었습니다. 검증된 보드 설정은 다음과 같습니다.

```text
esp32:esp32:esp32s3:CDCOnBoot=cdc,FlashSize=16M,PSRAM=opi,PartitionScheme=app3M_fat9M_16MB
```

```sh
arduino-cli core update-index --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
arduino-cli core install esp32:esp32@3.3.8 --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
arduino-cli lib install "GFX Library for Arduino@1.6.7" "SensorLib@0.4.1" "XPowersLib@0.3.3"
python -m pip install Pillow==12.3.0 fonttools==4.64.0
python tools/check_korean.py
python tools/check_web.py
node tools/tests/installer.test.cjs
```

실기 테스트는 [공개 설치 페이지](https://loaram.github.io/TamaPoke_ko/)에서 이미 빌드된 펌웨어로 시작할 수 있습니다. 코드를 고친 경우에만 다음으로 다시 빌드합니다.

```sh
python tools/build_firmware.py --publish
python tools/check_web.py
```

CLI가 PATH에 없다면 `--cli`에 실제 실행 파일 경로를 지정합니다. Windows 에뮬레이터는 LLVM-MinGW와 SDL2 2.32.10의 x86_64-w64-mingw32 패키지로 검증했습니다. `python tools/build_emulator.py --cxx <clang++경로> --sdl <SDL2경로>`로 경로를 지정할 수 있습니다. macOS/Linux는 C++17 컴파일러와 SDL2 개발 패키지를 설치한 뒤 기본 명령을 사용합니다.

```sh
python tools/unpack_sprites.py
python tools/build_emulator.py
python tools/test_runtime.py
```

정식 버전 작업을 마칠 때 비공개 도감 완성판도 함께 만듭니다.

```sh
python tools/build_emulator.py --full-dex
python tools/build_emulator.py --full-shiny
```

Android Studio의 SDK·NDK·JBR이 설치된 Windows에서는 다음으로 Full 디버그 APK를 다시 만들 수 있습니다.

```powershell
.\.venv\Scripts\python.exe tools\build_android.py
```

Windows의 `tools/test_runtime.py`도 `--cxx` 인자를 지원합니다. `build/`, 추출한 `tools/sdcard/mons/`, 다운로드한 도구와 캐시는 새 PC에서 재생성하는 파일입니다. 소스·글꼴·배포용 펌웨어·스프라이트 팩은 모두 저장소에 있습니다.

## 실기 테스트 후 이어갈 순서

1. [실기 체크리스트](docs/HARDWARE_TEST.ko.md)를 따라 확인하고, GitHub의 [실기 테스트 결과 양식](https://github.com/Loaram/TamaPoke_ko/issues/new?template=hardware-test.md)에 기기 버전·증상·사진·재현 순서를 남깁니다. 기록된 결과는 다른 PC에서도 읽을 수 있습니다.
2. 다음 Codex 작업에 저장소 주소와 해당 테스트 이슈 링크를 전달합니다. 먼저 실제 결과를 읽고 실패 항목을 재현합니다.
3. 번역 문제는 `localization/ko.json` 및 `i18n.cpp`, 이름은 `localization/names-ko.json`, 표시 문제는 `korean_text.h`와 해당 화면 코드를 확인합니다.
4. 이름 변경 후 `python tools/gen_korean_names.py`, 표시 문자열 변경 후 `python tools/gen_korean_font.py`, 이후 `python tools/check_korean.py`를 실행합니다.
5. 원인과 관련된 테스트를 실행하고 수정한 펌웨어를 빌드합니다. 새 펌웨어를 공개할 때는 `FW_VERSION`을 올리고 `--publish`로 바이너리·manifest·해시 정보를 함께 갱신합니다.
6. `main`의 `web/` 변경은 Pages를 배포합니다. 배포 후 실제 URL에서 새 버전·다운로드·세이브 영역 보호를 확인하고 인수인계 내용을 갱신합니다.

기존 `HANDOVER.md`의 Dragonair, 보드 버전, 3.x 개발 브랜치 등은 원본 작성자의 과거 기록입니다. 현재 사용자의 실기 결과로 해석하지 마세요.

## 설치·세이브 유지 조건

업데이트할 때 **Erase device를 선택하지 않습니다.** manifest는 부트로더(0), 파티션(0x8000), boot_app0(0xe000), 앱(0x10000)을 따로 기록합니다. NVS 0x9000..0xe000와 FFat 0x610000..0xff0000를 침범하지 않는지 `tools/check_installer.py`로 검사합니다.

`web/firmware/tamapoke.bin`은 빈 기기용 통합 이미지입니다. 기존 세이브 유지 업데이트에는 사용하지 않습니다. 실제 기기 세이브는 아직 백업받지 않았으며 이번 작업 자료에 포함되어 있지 않습니다.
