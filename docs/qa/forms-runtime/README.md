# 폼체인지 베타 검증 — 2026-09-06

기준: 정식 2.0.2 `5e923a5b9d79112f459bf2fbc197e8e52db2838f`에서 분리한 `codex/persistent-forms`, 작업 버전 3.0.0-beta.1. 공개 업로드 없음.

## 통과한 검사

- 전체 35개 런타임 검사. 구버전 26/30/34바이트 보관 데이터, 기존 배틀·진화·박스·터치·탐색·Android 시간 처리 검사 포함.
- 추가 폼 검사: 179개 전체의 타입·종족값·60/70/80 경계, 도감 종의 기존 진화 정체성 유지.
- 좋은 작별: 리자몽이 폼 없이도 성장 1,440분/레벨73에 작별 가능. 메가리자몽X여도 조건 동일. 메가플라엣테는 기존 일반 진화가 남아 있어 작별 불가.
- 실제 피해량의 드래곤 자속 적용, 화덕 오거폰의 불꽃 덩굴방망이 상성 계산, 아르세우스·실버디·게노세크트의 기술 타입 선택.
- 현재 포켓몬/파티/박스 폼 선택의 저장·재로드, 이동, 데려오기, 신규 개체의 폼 초기화, 알 수 없는 폼 ID 보존, 읽을 수 없는 보관 원본의 덮어쓰기 방지.
- 4,096바이트를 넘는 실제 폼 포함 백업이 6,144바이트 안에 들어가며, 패킷 손실을 넣은 전송 검사에서 모든 바이트를 복원.
- LAN 레코드에 폼 번호 포함, 대전용 레벨 보정이 이미 선택한 폼을 지우지 않음.
- 실제 그래픽/터치 코드의 선택 버튼·페이지 화살표·스와이프·잠금·뒤로 가기 검사.
- 179개의 일반색, 171개의 이로치 파일과 이로치 없는 8폼의 동일 폼 일반색 대체를 실제 PMD 로더로 확인. `web/forms.pak`는 350개 파일/83,962,278바이트. 두 APK에 같은 팩이 들어간 것을 해시로 대조.
- 한국어 문자열/글꼴 검사 및 설치 도구의 전송·실패 복구 검사 통과.

## 로컬 빌드

- Windows PC 전용 폼 테스트 에뮬레이터: 별도 `tamapoke-forms-beta.nvs`, 새로운 저장만 6마리/레벨80 테스트 구성.
- Android APK: ARM64/x86_64, versionCode 3000.
- Wear OS APK: ARM32/ARM64, versionCode 3001. 기존 Galaxy Watch4~9 통합 구성.
- 두 APK 모두 같은 기존 디버그 키, 서명 검사 및 16KB zip alignment 검사 통과.
- ESP32-S3: 스케치 2,157,855바이트/3,145,728바이트(68%), 전역 RAM 105,892바이트/327,680바이트(32%). 앱 파일 2,158,000바이트. 최종 소스와 해시는 `build-results.json`.

화면은 `mega.png`, `eternamax.png`, `mask.png`, `mask-stats.png`. 실행 로그는 이 폴더의 `.log`, 그림별 출처·해시는 `art-manifest.json`에 기록했습니다.

## 검증하지 않은 부분과 다음 배포 조건

실제 Android/워치/ESP32의 설치·전원·터치·프레임률·최대 동적 메모리·무선 연결은 미검증입니다. 전체 NVS 가져오기 도중 전원 차단의 원자적 복구도 보장하지 않습니다. 전체 폼의 개별 애니메이션을 사람이 하나씩 감상한 것은 아니며 모든 파일의 실제 파싱/Idle 로딩과 대표 화면을 확인했습니다.

정식 배포 요청 시 3.0.0으로 버전을 확정하고 APK versionCode를 베타보다 높이세요. 공개 펌웨어/manifest/문서·설명서와 설치 ZIP을 갱신하고, 플레이 PDF·기존 워치 설치 PDF·해시를 함께 첨부해야 합니다. 비공개 `-dex`/`-shiny` 에뮬레이터도 별도로 만드세요. 폼 팩과 크레딧·라이선스도 배포 대상에 포함해야 합니다. 이번 작업은 해당 정식 업로드 절차를 실행하지 않았습니다.
